# گزارش نهایی اصلاح و آزمون تغییرات

تاریخ: ۲۰۲۶-۰۹-۲۳

محیط اجرا: Windows، Python 3.13.14

نسخه برنامه: `0.3.0` — نسخه پروتکل: `2`

## نتیجه

- کامپایل تمام فایل‌های Python موفق بود.
- ۹۰ تست از ۹۰ تست موفق شد.
- آزمایش فشار: ۱۰۰۰ پاسخ از ۱۰۰۰ درخواست، بدون افت.
- آزمایش قطع: هنگام قطع ۰ از ۱۰ و پس از بازیابی ۳۰ از ۳۰ پاسخ دریافت شد.
- sandbox دوکاناله: ۱۰۰ از ۱۰۰ پاسخ، با رد مبدأ نادرست و HMAC دستکاری‌شده.

این اعداد فقط به آزمایش loopback تعلق دارند و ظرفیت، مسیریابی یا پذیرش مبدأ در شبکه عمومی را اثبات نمی‌کنند.

## ایرادهایی که پیدا و اصلاح شدند

1. پس از restart یک‌طرفه، sequence پایین‌تر می‌توانست برای همیشه replay تلقی شود. نسخه ۲ پروتکل اکنون `session_id` تصادفی و احراز‌شده دارد و sessionهای بازنشسته دوباره پذیرفته نمی‌شوند.
2. مقادیر `NaN` و `Infinity` برای timeoutها از validation عبور می‌کردند. اکنون فقط عدد متناهی و مثبت پذیرفته می‌شود.
3. health سمت ایران ممکن بود PONG قدیمی را به downlink تازه ترجیح دهد. اکنون تازه‌ترین زمان معتبر مبناست.
4. پاسخ سرویس داخلی سمت خارج از هر process محلی پذیرفته می‌شد. اکنون مبدأ دقیق `inner_peer` الزام می‌شود.
5. peer داخلی سمت ایران توسط یک sender محلی دیگر قابل جایگزینی بود. گزینه `expected_inner_peer` برای pin کردن IP و port ثابت اضافه شد.
6. ابزار آزمایش در burst بزرگ به‌علت buffer کوچک و دریافت دیرهنگام خودش packet از دست می‌داد. دریافت هم‌زمان و buffer چهار مگابایتی اضافه شد و پنج اجرای ۱۰۰۰تایی بدون افت طی توسعه موفق بودند.
7. شناسه درخواست در burstهای متوالی از صفر شروع می‌شد و پاسخ مانده از burst قبلی قابل اشتباه بود. شناسه‌ها اکنون در کل اجرای lab یکتا و افزایشی هستند.

## پوشش محیط شبیه‌سازی

محیط `scripts/lab_environment.py` دو relay یک‌طرفه و مستقل دارد: uplink و downlink. relay برگشت از `127.0.0.2` ارسال می‌کند تا بررسی مبدأ واقعی نیز پوشش داده شود. delay، jitter، drop، duplicate، reorder، outage و recovery به‌شکل کنترل‌شده قابل تزریق‌اند.

نتیجه دقیق هر پروفایل و تفسیر آن در `LAB_REPORT_FA.md` آمده و خروجی قابل پردازش در `LAB_REPORT.json` ذخیره شده است.

## محدودیت‌های باقی‌مانده

- loss واقعی جبران نمی‌شود؛ retransmission، FEC و congestion control وجود ندارد.
- HMAC محرمانگی ایجاد نمی‌کند و برای payload حساس باید لایه رمزنگاری مستقل استفاده شود.
- replay state در حافظه است و با restart کامل receiver از بین می‌رود.
- payload بزرگ ممکن است در اینترنت fragment شود؛ سقف نرم برنامه ۶۰۰۰۰ بایت است ولی مقدار عملی پیشنهادی حدود ۱۲۰۰ بایت است.
- اگر `expected_inner_peer` برابر `null` باشد، peer سمت ایران عمداً dynamic باقی می‌ماند.
- protocol v2 با v1 سازگار نیست و دو سمت باید هم‌زمان ارتقا یابند.

## خارج از دامنه آزمایش این میزبان

این سیستم Linux/WSL/Docker فعال ندارد؛ بنابراین systemd، iproute2 و iptables به‌صورت واقعی اجرا نشدند و اسکریپت‌های shell فقط با تست قرارداد و ممیزی ایستا بررسی شدند. هیچ endpoint عمومی، IP شخص ثالث یا ارسال زنده با مبدأ جعل‌شده آزمایش نشده است.
